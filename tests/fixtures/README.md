# Numerical fixtures

`generic-trainer.json` retains RCForge's original analytical trainer so existing
physics regressions keep their original inputs. It is not a bundled aircraft.
The selectable **Simple Trainer** is Vortex RC's design in
`aircraft/vt-simple-trainer.json`.
